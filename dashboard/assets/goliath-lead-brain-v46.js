function openStage(btn){const card=btn.closest('.leadCard');const box=card.querySelector('.stageDetail');const stage=btn.dataset.stage;const label=btn.innerText.trim();box.innerHTML='<h3>'+label+'</h3><p>Lead ID: '+card.dataset.lead+'</p><p>Status: '+(btn.classList.contains('done')?'Complete — click through worker output for artifact details.':'Waiting — next cron/agent run should create this artifact.')+'</p><p><a style="color:#ffd36b" href="/dashboard/goliath-worker-output.php">Open Worker Output →</a></p>';}
